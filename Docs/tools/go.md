# go

# Description

git open, opens the remote of the current repository

## Usage
```
usage: pt go [-h]

git open, opens the remote of the current repository

options:
  -h, --help  show this help message and exit
```