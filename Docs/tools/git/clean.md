# clean

# Description

clean merged remote branches

## Usage
```
Usage: pt git clean [-h] [-y]

clean merged remote branches

options:
  -h, --help        show this help message and exit
  -y, --no-confirm  If set, skip the confirmation prompt before committing
```

